;
; git configuration for devcontainer where git workspace is mounted from windows
;

[core]
    ; ignore case on Windows/devcontainer
    ignorecase = true
    ; ignore filemode on Windows/devcontainer
    filemode = false
    ; Crlf handling
    autocrlf = input
    safecrlf = false
