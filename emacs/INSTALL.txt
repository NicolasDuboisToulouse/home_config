* install
Checkout in <home>/.config/emacs

* Linux: 
# ln -s ~/.config/emacs/dot_emacs ~/.emacs

* Windows: 
 Use mklink as admin

** Windows context menu
   Edit emacs_context_menu.reg to setup emacs path
